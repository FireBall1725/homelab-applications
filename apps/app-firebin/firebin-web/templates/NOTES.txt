Thanks for installing firebin-web.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag }}

The web image's bundled nginx.conf listens on :3000 and proxies /api to
http://firebin-api:8080 by the bare service name, so firebin-web must be
deployed into the same namespace as firebin-api (app-firebin).

Open the UI:
{{- range .Values.ingress.main.hosts }}
  http://{{ .host }}
{{- end }}

Or port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "common.names.fullname" . }} 3000:3000
