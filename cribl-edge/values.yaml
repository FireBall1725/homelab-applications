edge:
  # Leader connection string - referenced from sealed secret
  # The actual token is encrypted in templates/sealed-secret.yaml
  env:
    - name: CRIBL_EDGE_LEADER
      valueFrom:
        secretKeyRef:
          name: cribl-edge-leader
          key: leader
