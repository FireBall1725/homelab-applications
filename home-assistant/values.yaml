home-assistant:
  persistence:
    enabled: true
    accessMode: ReadWriteOnce
    size: 100Gi

  ingress:
    enabled: true
    hosts:
      - host: ha.k8s.firekatt.ca
        paths:
          - path: /
            pathType: Prefix

  hostNetwork: true
